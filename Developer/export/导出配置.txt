Name:Create More Chunks
Version:

# 是否打包正式版 PCL，以便没有启动器的玩家安装整合包。
IncludeLauncher:False

# 是否打包 PCL 个性化内容，例如功能隐藏设置、主页、背景音乐和图片等。
IncludeLauncherCustom:False

# 是否将 Mod、资源包、光影包的文件直接放入整合包中，这样在导入时就无需联网下载它们。
# 建议仅在无法稳定连接 CurseForge 或 Modrinth 时才考虑启用。
# 二次分发可能违反使用协议，请尽量不要公开发布包含资源文件的整合包！
DontCheckHostedAssets:False

# 若资源未在 Modrinth 上托管，则将其原始文件直接放入整合包中。
# 如果你想将整合包上传到 Modrinth，这能帮助你分辨哪些文件未在 Modrinth 上托管。
# 此选项与 DontCheckHostedAssets 冲突。
ModrinthUploadMode:False

# 导出的文件的存放位置。
# 若设置了此项，在导出时会直接将文件放到此路径，不会弹窗要求选择。
# 若 IncludeLauncher 为 True，应以 .zip 结尾；若为 False，应以 .mrpack 结尾。
PackPath:

==============================================================

# 修改下方的规则以控制需要导出的内容。
# 以 ! 开头以反选。可以使用 *、?、[] 通配符。靠后的行覆盖靠前的。

# Mod
mods/
!mods/*.disabled
!mods/*.old
!mods/.connector/

# 整合包重要数据
hotai/
icon.png
kubejs/
!kubejs/probe/
!kubejs/jsconfig.json
!kubejs/README.txt
README.md
UpdateLogs.md
LICENSE.md
ldlib/

# Mod 设置
config/
!config/jei/world/
defaultconfigs/
CustomSkinLoader/CustomSkinLoader.json

# 导出的结构
schematics/

# 资源包
resourcepacks/Better Chunk by Chunk v1.4.zip

==============================================================

# 如果想将额外的文件自动放到压缩包根目录中，可以将它们的路径写在下方。
# 必须是完整路径。每行中，若以 \ 结尾则代表是文件夹，不以 \ 结尾则代表是文件。
